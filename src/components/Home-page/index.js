import React from 'react';
import SectionSpeakers from "../SectionSpeakers";

const Homepage = () => {
    return (
        <div>
            <SectionSpeakers/>
        </div>
    );
};

export default Homepage;